<%= render 'users/info_a', users: @users %>

<%= render 'users/new_book', users: @users %>

<h1>Users</h1>
<% @users.each do |user| %>
<div>
  <p>image</p>
  <%= attachment_image_tag user, :profile_image, :fill, 40, 40, fallback: "no_image.jpg" %>
  <p><%= user.name %></p>
  <%= link_to "Show", mypage_path(user.id) %>
</div>
<% end %>